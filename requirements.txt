# Mirrors pyproject.toml, which is the source of truth. Keep the bounds in sync —
# `pip install -r requirements.txt` must not resolve to a broken dependency set.
# The mcp upper bound is load-bearing: 2.0.0 removed `mcp.server.fastmcp`.
mcp[cli]>=1.2.0,<2
openai>=1.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Dev/test (optional but recommended)
pytest>=8.0.0
hypothesis>=6.100,<7
ruff>=0.5.0,<0.17.0
